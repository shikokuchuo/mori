% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/share.R
\name{unlink_shared}
\alias{unlink_shared}
\title{Unlink Shared Memory Regions}
\usage{
unlink_shared(name = NULL)
}
\arguments{
\item{name}{a character vector of shared memory names as returned by
\code{\link[=shared_name]{shared_name()}}, or \code{NULL} (the default) to reap orphaned regions. A
sub-object path (e.g. \code{"/mori_abc_1[2,3]"}) is accepted and resolves to
its underlying region. Names that are not valid mori identifiers are
ignored.}
}
\value{
Invisibly, a character vector of the region names that were
removed, or \code{NULL} if nothing was removed.
}
\description{
Remove shared memory regions created by \code{\link[=share]{share()}}. Pass one or more region
names to remove specific regions, or call with no arguments to reap
\emph{orphaned} regions: those whose creating process has died without cleaning
up, for example after a crash, a \code{SIGKILL}, or the out-of-memory killer.
}
\details{
Shared memory is normally managed automatically: a region is unlinked when
the \code{\link[=share]{share()}} object that owns it is garbage-collected, and on a clean R
session exit. A region is only left behind if the owning process is killed
before either can run. \code{unlink_shared()} removes such leftovers.

Unlinking a region removes only its name. Processes that have already mapped
it keep reading until they release it; the memory is freed once the last
mapping is gone.

The reap form (\code{name = NULL}) is \strong{conservative}: a region is removed only
if its creating process — encoded in the region name — is no longer
running, so regions still in use by a live process are never touched.
}
\section{Platform behaviour}{

\describe{
\item{Linux}{Both forms are supported; reaping enumerates \verb{/dev/shm}.}
\item{macOS}{Removal by name is supported. Reaping is not: the kernel
shared memory namespace cannot be enumerated, so names must be passed
explicitly.}
\item{Windows}{Nothing is removed. A file mapping is reference-counted by
the operating system and cannot be orphaned, so there is no leftover to
clean up.}
}
}

\examples{
x <- share(rnorm(100))
nm <- shared_name(x)
rm(x)
unlink_shared(nm)

}
\seealso{
\code{\link[=share]{share()}} to create a shared object, \code{\link[=shared_name]{shared_name()}} to extract a
region name.
}
